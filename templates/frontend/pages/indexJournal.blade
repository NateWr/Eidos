<x-eidostheme::layout
    title="{{ $currentContext->getLocalizedName() }}"
    description="{{ $currentContext->getLocalizedData('description') }}"
>
    <div class="page-container page-homepage">
        <h1 class="sr-only">
            {{ $currentContext->getLocalizedName() }}
        </h1>

        <x-homepage-blocks />
    </div>
</x-eidostheme::layout>
